services:
  - type: web
    name: zenithstream
    env: node
    plan: free
    buildCommand: pnpm run build
    startCommand: node dist/server/index.mjs
    envVars:
      - key: NODE_VERSION
        value: 22
