services:
  - type: web
    name: zenithstream
    env: node
    plan: free
    buildCommand: pnpm run build:render
    startCommand: node .output/server/index.mjs
    envVars:
      - key: NODE_VERSION
        value: 22
