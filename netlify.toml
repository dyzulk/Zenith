[build]
  command = "pnpm run build:netlify"
  publish = ".output/public"

[functions]
  directory = ".output/server"

[build.environment]
  NODE_VERSION = "22"

# Auto-deployment from git push is disabled
# Use manual deploy or API webhook to trigger deployments
